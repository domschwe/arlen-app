input AMPLIFY {
  globalAuthRule: AuthRule = {allow: public}
}

type Training @model {
  id: ID!
  name: String!
  instructor: [String]
}

type Instructor @model {
  id: ID!
  fName: String!
  lName: String!
}
 